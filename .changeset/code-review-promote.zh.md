---
"mattpocock-skills": minor
---

将 in-progress 的 **`review`** skill 重命名为 **`code-review`**，并从 `in-progress/` 提升到 `engineering/` bucket。它现在随 plugin 发布，列在 top-level 和 Engineering READMEs（Model-invoked）中，并拥有面向人的 docs page：`docs/engineering/code-review.md`。`/implement` skill 和 docs 现在指向 `/code-review`。
