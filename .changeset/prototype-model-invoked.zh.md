---
"mattpocock-skills": minor
---

让 **`prototype`** skill 变为 model-invoked，使 agent 可以自主触达它，其他 skills 也可以调用它。其 description 围绕 leading word _prototype_ 重写：用 throwaway code 回答 design question，并为每个 branch 提供一个 trigger（state/logic sanity-check 或 UI exploration）。
