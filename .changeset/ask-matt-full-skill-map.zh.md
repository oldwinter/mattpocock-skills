---
"mattpocock-skills": minor
---

将 **`ask-matt`** router 更新到完整 skill set。它现在补上此前缺失的五个 skills：**`tdd`**（作为 `implement` 驱动的 red-green engine 嵌入 main flow）、**`diagnosing-bugs`**（新的 “Something's broken” 入口，此前没有 bug route）、**`domain-modeling`** 和 **`codebase-design`**（新的 “Vocabulary underneath” section），以及 **`grilling`**（共享 interview primitive）。`prototype` 被扩展为 standalone；描述也从 “user-invoked skills” 扩展为 “the skills”。`CLAUDE.md` 增加 maintenance rule：未来任何 skill add/rename/remove 或 flow change，除了既有 docs-page re-sync，也必须重新检查 `ask-matt`。
